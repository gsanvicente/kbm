// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: staff.sql

package sqlcgen

import (
	"context"
)

const getStaffUserForLogin = `-- name: GetStaffUserForLogin :one
SELECT id, client_id, email, password_hash, role, is_active FROM users WHERE email = $1
`

type GetStaffUserForLoginRow struct {
	ID           string
	ClientID     *string
	Email        string
	PasswordHash string
	Role         UserRole
	IsActive     bool
}

func (q *Queries) GetStaffUserForLogin(ctx context.Context, email string) (GetStaffUserForLoginRow, error) {
	row := q.db.QueryRow(ctx, getStaffUserForLogin, email)
	var i GetStaffUserForLoginRow
	err := row.Scan(
		&i.ID,
		&i.ClientID,
		&i.Email,
		&i.PasswordHash,
		&i.Role,
		&i.IsActive,
	)
	return i, err
}

const getStaffUserIDByEmail = `-- name: GetStaffUserIDByEmail :one
SELECT id FROM users WHERE email = $1
`

// Resuelve el email que admin/ maneja (Session.email) al uuid real que
// las tablas de Tesorería/Aprobaciones/Reclamos exigen como
// requested_by/registered_by/etc. — ver
// docs/adr/0012-full-postgres-migration-clients-treasury-staff-approvals.md.
func (q *Queries) GetStaffUserIDByEmail(ctx context.Context, email string) (string, error) {
	row := q.db.QueryRow(ctx, getStaffUserIDByEmail, email)
	var id string
	err := row.Scan(&id)
	return id, err
}
